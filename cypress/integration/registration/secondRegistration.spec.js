
describe('Student choosing a mentor to chat with', function () {
    beforeEach(function () {
        // beforeEach function used so that it runs before each test
    });
    it('should fill registration form  ', function () {
        // happy path of filling the registration form
        // includes start chat. 
    });
    it('should display correct validation for fields', function () {
        // verify all validation names appear when clicking Start to chat
    });
    it('should validate passford field', function () {// Password - verify that when user types 4 letters - password must be at least 8 characters
        // Password -fill in 8 characters
        // Confirm password -verify that confirm password field says confirm password is a required field
        // Confirm password - verify that when password filled incorrectly to previous state it says it does not match
    });
});